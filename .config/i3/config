# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Backspace exec i3-sensible-terminal
bindsym $mod+Return "exec --no-startup-id urxvt"

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
#bindsym $mod+h split h
bindsym $mod+backslash split h
bindsym $mod+bar split h

# split in vertical orientation
#bindsym $mod+v split v
bindsym $mod+minus split v
bindsym $mod+underscore split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
# bindsym $mod+Shift+space floating toggle
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle
bindsym $mod+Shift+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
# bindsym $mod+2 workspace 2

# Icons
# Font Awesome
# http://fontawesome.io/cheatsheet/

set $ws1 "1. tmux ☯"
bindsym $mod+1 workspace $ws1
bindsym $mod+Shift+1 move container to workspace $ws1

set $ws2 "2. qt ⚓"
bindsym $mod+2 workspace $ws2
bindsym $mod+Shift+2 move container to workspace $ws2


set $ws3 "3. wch ⚓"
bindsym $mod+3 workspace $ws3
bindsym $mod+Shift+3 move container to workspace $ws3

set $ws4 "4. mt"
bindsym $mod+4 workspace $ws4
bindsym $mod+Shift+4 move container to workspace $ws4

bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
# bindsym $mod+Shift+2 move container to workspace 2


bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        status_command i3status
#	position top
#}

set $bumblebee $HOME/.config/i3/bumblebee-status/bumblebee-status
set $commandline -m memory time date -p root.path=/ date.format="%a, %b %d %Y" time.format="%H:%M" -t solarized-powerline
bar {

# strip_workspace_numbers yes
binding_mode_indicator yes
# separator_symbol "/"

  font pango:Inconsolata 8
    position top
#    tray_output none
# Modules:
# nic (3), memory, date, time
    status_command $bumblebee $commandline
#    status_command $bumblebee -m nic disk:root cpu memory battery date time dnf -p root.path=/ time.format="%H:%M CW %V" date.format="%a, %b %d %Y" -t solarized-powerline
#time pasink pasource
  colors {
    separator #666666
    background #000000
    statusline #ffffff
    focused_workspace #4c7899 #285577 #ffffff
    active_workspace #333333 #5f676a #ffffff
    inactive_workspace #333333 #222222 #888888
    urgent_workspace #2f343a #900000 #ffffff
  }
}

set $Locker i3lock && sleep 1

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
#    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym l exec --no-startup-id i3lock --color=87ceeb --image "$HOME/Tengerid Lockscreen.png", mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Pause mode "$mode_system"


workspace_layout tabbed
hide_edge_borders both

# bar toggle, hide or show
bindsym $mod+m bar mode toggle

bindsym --release Print exec --no-startup-id scrot --select 'Screenshot-%b-%d-%y_%H-%m-%S_$wx$h.png' -e 'mv $f ~/.scrot/'
bindsym Shift+Print exec --no-startup-id scrot
bindsym XF86WebCam exec --no-startup-id slip -r
bindsym $mod+XF86WebCam exec --no-startup-id slip -q
bindsym $mod+d exec "rofi -combi-modi drun,run -show combi"
bindsym $mod+w exec "rofi -show window"
#bindsym Mod1+x exec --no-startup-id i3lock --color=87ceeb --image "$HOME/Tengerid Lockscreen.png"
bindsym Ctrl+Mod1+l exec --no-startup-id i3lock --color=87ceeb --image "$HOME/Tengerid Lockscreen.png"
# --grayscale, --text
bindsym Ctrl+Mod1+b exec --no-startup-id i3lock-fancy -g -t "STAY AWAY !!!"
bindsym Mod1+b exec --no-startup-id i3lock --color=87ceeb --image "$HOME/SatanicBear.png"

exec --no-startup-id redshift-gtk
# exec --no-startup-id dropbox start

# Set GNOME Network Manager applet in the tray
exec --no-startup-id nm-applet

exec --no-startup-id clipit

# dunst -config $C/dunst/dunstrc
exec --no-startup-id dunst

# Set wallpaper
exec --no-startup-id feh --bg-scale "$HOME/Tengerid Wallpaper.png"

# Disable touchpad
exec --no-startup-id xinput --disable "ETPS/2 Elantech Touchpad"

# Set keyboard layout
exec --no-startup-id setxkbmap -layout "us,ru,az" -option "grp:alt_shift_toggle"

# Remap escape to Caps lock
# Might esp. be useful on Vim being in the same home row as hjkl nav. keys
# exec --no-startup-id setxkbmap -option caps:escape
# Keep Esc as Caps Lock
exec --no-startup-id setxkbmap -option "caps:swapescape"

# Keyboard layout indicator hanging on tray
exec --no-startup-id fbxkb

# Read Xresources configuration
exec --no-startup-id xrdb -merge $C/Xresources

# Turn off NumLock key
exec --no-startup-id numlockx off

# Set keyboard layout
exec --no-startup-id setxkbmap -layout "us" -option "grp:alt_shift_toggle"

# Start PN Mixer volume controller (hangs on tray)
exec --no-startup-id pnmixer

# Battery status indicator on tray
# exec --no-startup-id batterymon

# exec --no-startup-id urxvtd

# Rename workspaces
bindsym $mod+z exec --no-startup-id i3-input -F 'Rename workspace to "%s"' -P 'New name: '

set $port 6611
# set $port $MPD_PORT

bindsym XF86AudioPlay exec --no-startup-id mpc -p $port toggle
bindsym XF86AudioStop exec --no-startup-id mpc -p $port stop
bindsym XF86AudioPrev exec --no-startup-id mpc -p $port prev
bindsym XF86AudioNext exec --no-startup-id mpc -p $port next

# Use xbindkeys -k to find key codes

bindsym XF86ScreenSaver exec --no-startup-id xclock
bindsym XF86Calculator exec --no-startup-id xcalc 


# ---

# Brightness

# Install light-git from AUR
# as xbacklight -inc 20 outputs error
# No outputs have backlight property

# -p increases by percent, rather than raw (-r)

# Get value by light -G

bindsym XF86MonBrightnessUp exec "light -A -p 20"
bindsym XF86MonBrightnessDown exec "light -U -p 20"

# ---

# Volume

# Global volume control
set $step 5
bindsym XF86AudioRaiseVolume exec amixer set Master $step%+
bindsym XF86AudioLowerVolume exec amixer set Master $step%-
# Global mute
bindsym XF86AudioMute exec amixer sset Master toggle

# ---

# MPD volume control
# set $step 5
# bindsym XF86AudioRaiseVolume exec "mpc -p $port volume +$step"
# bindsym XF86AudioLowerVolume exec "mpc -p $port volume -$step"
# bindsym XF86AudioMute exec mpc volume 0

# ---

# Cycle through workspaces
bindsym $mod+grave workspace next
bindsym $mod+bracketright workspace next
bindsym $mod+bracketleft workspace prev


# Bring up web browser
bindsym $mod+F1 exec --no-startup-id  qutebrowser --backend webengine

# Bring up file manager
bindsym $mod+Shift+Return exec --no-startup-id  urxvt -e ranger

exec --no-startup-id notify-send "Welcome to the System, My Lord!"

client.focused #4c7899 #285577 #ffffff #2e9ef4
client.focused_inactive #333333 #5f676a #ffffff #484e50
client.unfocused #333333 #222222 #888888 #292d2e
client.urgent #2f343a #900000 #ffffff #900000
